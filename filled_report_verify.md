# 基于大模型与知识图谱的《封神演义》增强型 GraphRAG 问答系统

项目报告

学号：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\
姓名：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\
专业：计算机科学与技术\
年级：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\
班级：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

# 摘要

传统 RAG
系统主要依赖向量相似度检索，能够较好地回答与原文片段高度相似的事实性问题，但在人物关系、多跳推理、图谱溯源和边界拒答等场景中容易出现证据分散、关系链不完整或模型幻觉等问题。针对课程"基于大模型 +
知识图谱的增强型 RAG
问答系统"要求，本项目以《封神演义》为领域语料，构建了一个结合大模型知识抽取、Neo4j
知识图谱、FAISS 向量索引、智能查询路由、GraphRAG 多跳检索和 Streamlit
可视化界面的问答系统。

系统首先对《封神演义》原文进行章节解析、文本清洗和文本切块，满足 500
条以上非结构化文本数据处理要求；随后通过大模型 Prompt
对文本片段进行命名实体识别（NER）和关系抽取（RE），将人物、教派、地点、法宝、坐骑、阵法、事件、神位等实体以及师承、阵营、亲属、对抗、法宝、事件参与、封神结局等关系写入
Neo4j 图数据库，并建立索引和别名合并策略。在线问答阶段，系统使用 FAISS
对原文片段进行 Top-K
向量检索，同时使用图谱检索实现实体链接、一跳/多跳邻居检索、路径检索和子图提取，再由智能路由模块根据问题复杂度选择传统混合检索、GraphRAG
检索或组合策略。最终，大模型只基于"原文摘录 +
图谱三元组"的证据上下文生成带引用编号的回答，Streamlit
前端同步展示答案、原文片段来源和知识图谱子图。

目前项目已完成从数据处理、图谱构建、向量检索、图谱检索、问答生成到 Web
展示的完整链路。已生成的抽取缓存样例覆盖前 20 回，包含 350 个实体、420
条关系；全书数据包含 99 回、约 60 万字，可按段落进一步写入 500 条以上
TextChunk
原文片段。系统能够回答"纣王是谁""纣王和女娲有什么关系"等简单事实问题，也能回答"哪吒的师父、教派和法宝"等多跳关系问题，并在"孙悟空在《封神演义》中有什么法宝"一类边界问题上基于证据不足进行拒答。实验结果表明，与纯向量
RAG 相比，GraphRAG
在关系组织、多跳推理、答案可解释性和溯源可视化方面具有明显优势。

# 目录

1.  项目背景

2.  项目需求分析

3.  项目整体设计

4.  项目详细设计

5.  项目实现

6.  项目总结和展望

# 1. 项目背景

## 1.1 传统 RAG 的局限

检索增强生成（Retrieval-Augmented Generation,
RAG）通过外部知识库检索相关文本片段，并将检索结果作为上下文交给大模型生成答案。传统
RAG
的核心优势在于能够降低模型依赖内部参数记忆的程度，并将回答锚定到外部文档证据上。然而，在实际应用中，仅依赖向量检索存在以下不足：

1.  **多跳关系难以稳定召回。**
    向量检索通常以文本片段为单位返回语义相近内容。如果一个问题需要先找到实体
    A，再沿着关系找到实体
    B、C，相关信息可能分布在多个片段中，向量检索未必能一次性召回完整链路。

2.  **结构化关系表达不足。**
    文本片段中隐藏着人物师承、阵营归属、法宝拥有、事件参与等关系，但传统
    RAG
    只将其作为自然语言文本处理，缺少显式的"头实体---关系---尾实体"结构。

3.  **可解释性和可视化溯源不足。**
    用户只能看到若干文本片段，难以观察模型推理时涉及了哪些实体、关系和路径。

4.  **容易产生证据外推。**
    当问题与相似文本片段存在表面相似但事实不匹配时，模型可能根据自身知识或相似语境补全答案，导致幻觉。

课程任务要求构建一个"基于大模型 + 知识图谱"的增强型 RAG
系统，利用知识图谱的结构化表达能力补足传统 RAG
在逻辑推理和多跳查询上的不足。因此，本项目选择《封神演义》作为领域文本，构建面向古典神魔小说知识问答的
GraphRAG 系统。

## 1.2 选题意义

《封神演义》人物众多，包含商、周、阐教、截教、西方教等多个阵营，涉及大量师徒、亲属、对抗、法宝、阵法、战役和封神结局关系，非常适合验证知识图谱在复杂关系问答中的价值。例如，用户询问"哪吒的师父是谁？他属于哪个教派？有哪些法宝？"时，系统需要同时处理人物节点、师承关系、教派归属关系和法宝拥有关系；询问"姜子牙和元始天尊是什么关系？"时，系统需要从图谱中提取师承路径并结合原文证据回答。

通过本项目，可以将自然语言文本处理、知识抽取、图数据库建模、向量检索、图谱检索、大模型生成和
Web 可视化整合到一个完整系统中，体现图机器学习与知识图谱技术在增强型 RAG
问答中的应用价值。

# 2. 项目需求分析

## 2.1 课程任务目标对应

本项目围绕课程要求，将系统功能划分为四个层次：数据处理与知识图谱构建、混合检索引擎、问答生成与逻辑推理、用户交互界面。

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  课程要求                   本项目实现方式                                                                                    对应文件/模块
  -------------------------- ------------------------------------------------------------------------------------------------- -----------------------------------------------------------------
  至少 500                   使用《封神演义》99 回原文，约 60 万字；按章节与段落切分为 TextChunk 原文片段                      封神演义.txt，scripts/import_fengshen_to_neo4j.py
  条非结构化文本数据                                                                                                           

  文本清洗、去噪、分句       按"第X回"解析章节边界，按段落、句号、问号、感叹号、换行等断点切分文本                             parse_chapters()，split_chapter_text()，\_persist_text_chunks()

  大模型实体识别与关系抽取   设计 NER + RE Prompt，抽取实体、关系和 evidence 原文证据                                          rag_modules/fengshen_kg_extraction.py

  输出标准三元组             关系统一保存为 (source_entity, relation, target_entity)，并附 evidence、chapter、chunk 等属性     ExtractedRelation，Neo4j 关系写入逻辑

  图数据库存储               使用 Neo4j 保存                                                                                   scripts/import_fengshen_to_neo4j.py
                             Person、Faction、Location、Artifact、Beast、Formation、Event、DeityPosition、Chapter、TextChunk   
                             等节点                                                                                            

  节点索引                   为实体 id、实体 name、TextChunk chunk_id、Chapter chapter_id 创建索引                             \_create_indexes()

  实体对齐                   根据 alias 对同类型重复实体进行合并                                                               \_merge_aliased_entities()

  向量检索                   使用 Embedding 模型向量化文本块，FAISS 保存本地向量索引，支持 Top-K 检索                          rag_modules/faiss_index_construction.py

  图谱检索                   实现实体链接、多跳邻居、路径、子图和三元组事实检索                                                rag_modules/graph_rag_retrieval.py

  检索融合                   融合 BM25、FAISS、图谱实体级/主题级结果，生成统一证据上下文                                       rag_modules/hybrid_retrieval.py

  问答 Pipeline              基于检索文档构造带编号证据上下文，调用大模型生成答案                                              rag_modules/generation_integration.py

  防幻觉 Prompt              要求只基于原文摘录和图谱事实回答，信息不足时明确无法确定                                          generate_adaptive_answer()

  Web 界面                   Streamlit 页面，提供问答输入、答案输出、引用来源和知识子图可视化                                  app.py，ui/styles.py
  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

## 2.2 功能划分

系统主要功能如下：

1.  **数据处理功能。**
    读取《封神演义》文本，解析章节标题和正文范围，按语义断点切分为可处理文本块；同时在
    Neo4j 中建立 Chapter 与 TextChunk 节点，保证后续答案能够回溯到原文。

2.  **知识抽取功能。**
    对每个文本块调用大模型进行实体识别和关系抽取。实体类型包括人物、教派/势力、地点、法宝、坐骑/灵兽、阵法、事件/战役、神位/封号；关系类型覆盖师承、阵营、亲属、对抗、法宝流转、事件参与、阵法部署、封神结局和通用关联。

3.  **知识图谱存储功能。** 将抽取出的实体和关系写入 Neo4j，并保存
    evidence、source_text、chapter_number、chapter_title、chunk_index
    等元数据；创建查询索引，并通过 alias 合并重复实体。

4.  **向量检索功能。** 将 TextChunk 或从 Neo4j 导出的 RAG
    文档嵌入为向量，存入 FAISS 本地索引，实现基于语义相似度的 Top-K
    文本块召回。

5.  **图谱检索功能。**
    从用户问题中提取候选实体，映射到图谱节点；根据问题类型执行实体直连、多跳邻居、路径查找或子图检索，得到三元组事实和推理链。

6.  **智能路由功能。**
    分析用户问题复杂度、关系密集度和推理需求，自动选择传统混合检索、GraphRAG
    检索或组合策略。

7.  **问答生成功能。**
    将原文片段和图谱事实合并为结构化证据上下文，调用大模型生成自然语言答案，并要求每个关键事实附带引用编号。

8.  **可视化交互功能。** 使用 Streamlit 构建 Web
    页面，展示系统指标、问答结果、引用来源卡片和知识图谱子图，使用户能够观察答案依据。

## 2.3 非功能性需求

除功能需求外，本项目还考虑以下非功能性要求：

- **可复现性。** 抽取结果保存到 data/fengshen/extraction_results，可通过
  \--skip-extraction 复用缓存，避免重复调用大模型。

- **可扩展性。** Schema、关系类型、检索模块和 UI
  展示模块相互解耦，后续可扩展到其他文学作品或领域文本。

- **可解释性。** 关系边保存
  evidence；生成答案必须带引用编号；前端展示原文摘录和图谱子图。

- **鲁棒性。**
  知识抽取和问答生成均提供异常处理和降级策略；当没有证据时，系统返回"根据当前资料无法完全确定"。

- **安全性。** 配置文件通过 .env 读取 API Key 和 Neo4j
  密码，不将敏感信息写入代码。

# 3. 项目整体设计

## 3.1 系统总体架构

本系统采用"离线图谱构建 +
在线检索问答"的双阶段架构。离线阶段从《封神演义》原文中抽取知识并构建图谱与向量索引；在线阶段根据用户问题进行智能路由，融合向量检索和图谱检索结果，再由大模型生成带证据的回答。

![图1 系统总体架构图](media/rId29.png){width="5.475694444444445in"
height="3.0817147856517937in"}

图1 系统总体架构图

图1展示了系统从数据层到展示层的完整链路。数据层包含《封神演义.txt》原始语料和
LLM 抽取缓存；知识图谱构建层负责章节解析、文本切块、实体关系抽取、Schema
校验和实体对齐；存储层由 Neo4j 知识图谱和 FAISS
向量索引组成；检索与路由层提供 GraphDataPreparation、Hybrid
Retrieval、GraphRAG Retrieval 和 Intelligent Query
Router；生成与证据层负责上下文融合、防幻觉生成和引用溯源；展示层通过
Streamlit Web UI 输出答案、原文片段和知识子图。

## 3.2 技术栈选型

  ---------------------------------------------------------------------------------------------------
  层次                   技术/框架                 选型理由
  ---------------------- ------------------------- --------------------------------------------------
  编程语言               Python                    生态丰富，适合文本处理、图数据库访问、向量检索和
                                                   Web 原型开发

  大模型 API             SiliconFlow 兼容 OpenAI   支持 Chat Completions 接口，便于知识抽取和问答生成
                         SDK，默认                 
                         deepseek-ai/DeepSeek-V3   

  Embedding 模型         BAAI/bge-m3               支持中文语义检索，适合长文本 RAG 场景

  图数据库               Neo4j                     Cypher
                                                   查询表达能力强，适合实体关系、路径和子图检索

  向量数据库             FAISS                     本地轻量、高效，适合课程项目快速构建 Top-K
                                                   相似检索

  检索框架               LangChain Document /      便于统一封装 RAG 文档、BM25、FAISS 和生成模块
                         Retriever 接口            

  前端界面               Streamlit                 快速构建问答界面，支持图表、HTML 组件和交互式展示

  图可视化               PyVis / HTML 网络图       可在 Streamlit 中展示知识子图节点和连线

  配置管理               .env + config.py          管理 API Key、Neo4j 连接、模型和索引路径
  ---------------------------------------------------------------------------------------------------

## 3.3 数据流设计

系统数据流分为离线构建流和在线问答流。

**离线构建流：**

1.  读取《封神演义》原文。

2.  使用 parse_chapters() 按章节标题定位 99 回边界。

3.  使用 split_chapter_text() 或 \_persist_text_chunks()
    按段落和标点切分文本。

4.  对文本块调用 FengshenKGExtractor，执行 NER 和 RE。

5.  按 gold_schema.py 过滤实体类型和关系类型。

6.  将实体、关系、章节、原文片段写入 Neo4j。

7.  基于 alias 进行实体对齐。

8.  从 Neo4j 转换 RAG 文档，构建 FAISS 向量索引。

**在线问答流：**

1.  用户在 Streamlit 页面输入问题。

2.  IntelligentQueryRouter
    分析问题复杂度、关系密集度、候选实体和推理需求。

3.  简单事实问题走传统混合检索；多跳关系问题走
    GraphRAG；复杂问题走组合策略。

4.  系统召回相似原文片段、图谱三元组、路径或子图。

5.  source_evidence 与 snippet_service 构造带编号证据上下文。

6.  GenerationIntegrationModule 调用大模型生成答案。

7.  前端展示答案、引用片段和知识图谱子图。

![图2 报告用系统流程简图](media/rId34.png){width="5.475694444444445in"
height="3.0817147856517937in"}

图2 报告用系统流程简图

图2用更紧凑的形式概括了系统流程：离线阶段构建 Neo4j 与
FAISS；在线阶段由智能路由同时连接两个知识源，并将原文片段和图谱事实融合后交给大模型回答。

# 4. 项目详细设计

## 4.1 数据处理与知识图谱构建设计

### 4.1.1 原始数据与文本切分

本项目使用《封神演义》全文作为领域语料。语料具有以下特点：

- 共 99 回，约 60 万字；

- 文本中包含大量人物、教派、法宝、阵法、事件和地名；

- 多数关系以叙事方式隐含在长段落中，需要大模型辅助抽取；

- 原文适合按章节和段落切分，能够满足 500 条以上非结构化文本数据要求。

章节解析函数 parse_chapters() 使用正则表达式识别"第X回
标题"的章节标题，返回章节号、标题、起始位置和结束位置。文本切分函数
split_chapter_text()
优先在句号、问号、感叹号、换行、分号等语义断点处截断，避免硬切分造成语义破坏。Neo4j
导入脚本中的 \_persist_text_chunks()
进一步将章节正文按段落合并为长度适中的 TextChunk，并建立
(TextChunk)-\[:BELONGS_TO_CHAPTER\]-\>(Chapter) 关系，便于原文溯源。

### 4.1.2 知识抽取 Prompt 设计

知识抽取模块位于 rag_modules/fengshen_kg_extraction.py。系统 Prompt
将大模型设定为《封神演义》知识图谱构建专家，并明确实体类型、关系类型、抽取规则和
JSON 输出格式。

实体类型包括：

  --------------------------------------------------------------------------------------
  实体类型               中文含义               示例
  ---------------------- ---------------------- ----------------------------------------
  Person                 人物                   纣王、姜子牙、哪吒、杨戬、元始天尊

  Faction                教派/势力              商、周、阐教、截教、西方教

  Location               地点                   朝歌、西岐、玉虚宫、碧游宫、陈塘关

  Artifact               法宝/物件              打神鞭、乾坤圈、混天绫、风火轮、翻天印

  Beast                  坐骑/灵兽              四不像、哮天犬、五色神牛

  Formation              阵法                   十绝阵、九曲黄河阵、诛仙阵、万仙阵

  Event                  事件/战役              女娲宫进香、哪吒闹海、武王伐纣

  DeityPosition          神位/封号              三坛海会大神、文曲星
  --------------------------------------------------------------------------------------

关系类型包括：

- 师承关系：MASTER_OF、APPRENTICE_OF；

- 教派与阵营：BELONGS_TO_SECT、FIGHTS_FOR；

- 亲属婚姻：FATHER_OF、CHILD_OF、BROTHER_OF、MARRIED_TO；

- 战斗对抗：KILLS、DEFEATS、CAPTURES、OPPOSES；

- 法宝/坐骑流转：OWNS、BESTOWS、LOSES、STEALS；

- 阵法与事件：CREATES、DEPLOYS、BREAKS、PARTICIPATES_IN、OCCURS_IN、LEADS、INITIATES；

- 封神结局：LISTED_ON、BECOMES；

- 通用关系：ALLIES_WITH、BETRAYS、RELATED_TO、MENTIONS。

Prompt
特别强调三条约束：第一，只抽取文本片段中明确出现或可严格推断的事实；第二，每条关系必须给出
evidence 原文证据；第三，输出必须是严格
JSON，便于程序解析和写入数据库。这些约束可以减少大模型在知识图谱构建阶段的幻觉。

### 4.1.3 图谱构建流程

![图3 知识图谱构建流程图](media/rId41.png){width="4.611111111111111in"
height="6.1481474190726155in"}

图3 知识图谱构建流程图

图3展示了知识图谱构建的详细流程。系统读取《封神演义.txt》后先解析章节，再进行文本清洗与切块；随后调用大模型抽取实体和关系，并判断返回结果是否为合法
JSON。如果解析失败，则记录错误并重试或跳过；如果解析成功，则分别校验实体类型和关系类型，写入
Neo4j 实体节点与关系边。同时，文本块作为 TextChunk 节点单独入库，并与
Chapter 节点关联。最后系统创建节点索引，基于 alias
合并重复实体，并保存或复用抽取缓存。

### 4.1.4 Neo4j 图数据库建模

Neo4j 中主要包含以下节点：

- Person：人物节点；

- Faction：教派或政治势力节点；

- Location：地点节点；

- Artifact：法宝或重要物件节点；

- Beast：坐骑或灵兽节点；

- Formation：阵法节点；

- Event：事件或战役节点；

- DeityPosition：神位或封号节点；

- Chapter：章节节点；

- TextChunk：原文片段节点。

每个实体节点保存
name、alias、description、entity_type、source_chunk_id、source_text、chapter_number、chapter_title
等属性。关系边保存
evidence、confidence、source_chunk_id、source_text、chapter_number、chapter_title
等属性。这样，图谱不仅保存结构化关系，还能回溯到支持该关系的原文证据。

系统在导入阶段创建索引：

- 各实体类型的 id 字段索引；

- 各实体类型的 name 索引；

- TextChunk(chunk_id) 索引；

- Chapter(chapter_id) 索引。

实体对齐通过 \_merge_aliased_entities() 实现。该函数遍历同类型实体的
alias
列表，如果多个节点共享别名，则将其合并到规范节点中，以减少"姜子牙/姜尚/子牙"等别名造成的图谱冗余。

### 4.1.5 知识图谱 Schema

![图4 知识图谱 Schema 示意图](media/rId46.png){width="5.1875in"
height="3.4583333333333335in"}

图4 知识图谱 Schema 示意图

图4展示了项目的图谱
Schema。人物是图谱核心，围绕人物连接教派、地点、法宝、坐骑、阵法、事件和神位。TextChunk
与 Chapter 形成原文证据链，并通过 MENTIONS
关系连接到人物、事件、法宝等实体。该设计同时满足"实体关系推理"和"原文证据溯源"两类需求。

## 4.2 向量检索模块设计

向量检索模块位于 rag_modules/faiss_index_construction.py。该模块使用
LangChain 的 FAISS 向量库封装，主要功能包括：

1.  **构建向量索引。** build_vector_index(documents) 将 RAG
    文档转换为向量并保存到本地 FAISS 目录。

2.  **加载向量索引。** load_collection()
    从本地目录加载已有索引，避免重复构建。

3.  **索引作用域校验。** scope_manifest.json 保存文档数量和
    fingerprint，确保当前文档集合与已保存索引一致，防止复用错误索引。

4.  **相似度检索。** similarity_search_with_score(query, k) 返回 Top-K
    相似文档和相似度分数。

5.  **LangChain Retriever 兼容。** get_retriever() 返回可与其他 RAG
    模块组合的检索器接口。

向量检索的优势在于能够快速召回与问题语义相关的原文片段。例如用户询问"女娲做了什么"，FAISS
能召回包含"女娲采五色石补青天""女娲宫进香"等内容的原文片段，为最终答案提供直接文本依据。

## 4.3 图谱检索模块设计

图谱检索模块位于
rag_modules/graph_rag_retrieval.py。该模块将自然语言问题映射为图查询意图，支持以下查询类型：

  ------------------------------------------------------------------------------------
  查询类型               含义                         示例
  ---------------------- ---------------------------- --------------------------------
  entity_relation        查询实体之间的直接关系       姜子牙和元始天尊是什么关系？

  multi_hop              查询需要一跳以上关系链       哪吒的师父是谁，他有哪些法宝？

  subgraph               查询某个实体周围的局部子图   展示哪吒相关人物和法宝

  path_finding           查询两个实体之间的路径       姜子牙如何关联到武王伐纣？

  clustering             查询社团或主题团簇           某阵营相关人物有哪些？
  ------------------------------------------------------------------------------------

模块核心流程如下：

1.  从问题中提取候选实体；

2.  根据启发式规则或大模型判断查询类型；

3.  将自然语言关系词映射到允许的关系类型，如"师父"映射为
    MASTER_OF，"法宝"映射为 OWNS；

4.  执行 Neo4j 查询，获取节点、关系、路径或子图；

5.  将图谱结果封装为 LangChain Document，并在 metadata 中保存
    subgraph_edges、entity_name、source_text 等信息；

6.  返回给路由器和生成模块。

图谱检索的价值在于显式利用结构化关系。例如"哪吒的师父、教派和法宝"可以沿
MASTER_OF、BELONGS_TO_SECT、OWNS
等边组织答案，不再完全依赖模型从多个文本片段中自行拼接。

## 4.4 混合检索与智能路由设计

### 4.4.1 混合检索

混合检索模块位于 rag_modules/hybrid_retrieval.py，采用"实体级 + 主题级 +
文本级"的双层/多层检索范式：

- **实体级检索。**
  根据问题中的人物、教派、法宝、事件等实体关键词查找图谱节点，并补充邻居节点。

- **主题级检索。**
  根据"师承、法宝、阵营、封神、破阵"等主题关键词查找相关关系。

- **BM25/FAISS 文本检索。** 召回相似原文片段，提供自然语言证据。

- **Round-robin 融合。**
  对不同来源结果进行去重、排序和轮询合并，避免单一检索器主导上下文。

- **证据优先排序。** 优先保留包含原文 evidence 或 source_text 的结果。

### 4.4.2 智能查询路由

智能路由模块位于
rag_modules/intelligent_query_router.py。它将用户问题分析为以下特征：

- 查询复杂度：简单查找、中等复杂度、多跳推理；

- 关系密集度：是否涉及多个实体或关系网络；

- 是否需要推理：是否需要关系链条或对比分析；

- 实体数量：问题中出现的明确实体数量；

- 推荐策略：hybrid_traditional、graph_rag 或 combined；

- 推荐理由和置信度。

路由逻辑如下：

1.  如果问题是简单实体介绍，如"纣王是谁"，优先走传统混合检索，以原文片段为主；

2.  如果问题包含"关系、师父、属于、法宝、参与、路径、哪些"等关系推理词，优先走
    GraphRAG；

3.  如果问题既需要文本背景又需要图谱关系，则走组合策略，同时调用向量检索和图谱检索；

4.  当大模型路由失败时，系统使用规则降级分析，保证系统仍可返回结果。

![图5 GraphRAG 检索融合流程图](media/rId54.png){width="5.1875in"
height="2.91951990376203in"}

图5 GraphRAG 检索融合流程图

图5展示了用户问题从问题分析到检索策略选择，再到上下文融合和答案生成的流程。文本检索路径提供原文依据，图谱检索路径提供关系推理结构，两者共同进入证据上下文。

## 4.5 问答生成与防幻觉设计

问答生成模块位于 rag_modules/generation_integration.py。系统首先调用
build_evidence_bundles() 和 format_evidence_context_for_generation()
将检索文档整理为带编号的证据上下文。每条证据可以包含：

- 原文摘录；

- 图谱事实；

- 来源标题；

- 检索类型；

- 原始 chunk id；

- source text；

- 子图关系。

生成 Prompt 的核心要求如下：

1.  只基于给定检索信息回答，不补充外部知识；

2.  优先依据"原文摘录"回答，原文摘录是验证观点的第一依据；

3.  图谱事实只辅助组织人物、法宝、事件关系，不能替代原文证据；

4.  每个关键事实后必须标注 \[1\]、\[2\] 等证据编号；

5.  信息不足时明确说明"根据当前资料无法完全确定"；

6.  不输出内部关系代码、chunk id 或数据库字段名；

7.  结构化呈现：先直接回答，再列出"原文依据"。

该设计使答案能够被用户追踪和核验，同时降低模型编造未检索事实的风险。当
API 调用失败时，系统使用 build_local_fallback_answer()
从检索文档中组织简要回答，保证系统可用性。

## 4.6 用户界面与可视化设计

前端界面位于 app.py 和 ui/styles.py。系统使用 Streamlit
构建网页，主要区域包括：

1.  **系统初始化区。** 用户输入 Neo4j
    连接信息并点击"初始化系统"；系统加载 RAG 模块、FAISS
    索引和图谱检索器。

2.  **问题输入区。** 用户输入自然语言问题，设置 Top-K 检索参数。

3.  **答案展示区。**
    显示大模型生成的回答，并用徽章展示检索策略、文档数量和证据状态。

4.  **原文溯源区。** 将回答中的引用编号映射到原文片段，展示来源卡片。

5.  **知识子图区。** 根据检索文档 metadata 中的 subgraph_edges 构建
    PyVis 网络图，展示实体节点和关系连线。

6.  **图谱清洗展示。** UI 中隐藏 source filename、chunk id、community id
    等内部节点，只展示
    Person、Faction、Location、Artifact、Beast、Formation、Event、DeityPosition、Chapter
    等可读节点。

![图6 用户问答时序图](media/rId60.png){width="5.475694444444445in"
height="3.0817147856517937in"}

图6 用户问答时序图

图6展示了用户从提问、路由、检索、生成到溯源展示的完整时序。系统不是直接让大模型回答，而是先通过智能路由决定检索策略，再将证据上下文交给生成模块，最后在前端展示答案和溯源信息。

# 5. 项目实现

## 5.1 项目目录结构

项目主要目录如下：

app.py \# Streamlit Web UI\
config.py \# 系统配置\
封神演义.txt \# 原始语料\
.env.example \# 环境变量模板\
rag_modules/\
fengshen_kg_extraction.py \# 大模型知识抽取模块\
gold_schema.py \# 实体类型与关系类型 Schema\
graph_data_preparation.py \# Neo4j 转 RAG 文档\
faiss_index_construction.py \# FAISS 向量索引构建与检索\
hybrid_retrieval.py \# BM25/FAISS/图谱混合检索\
graph_rag_retrieval.py \# 图谱实体链接、多跳、路径、子图检索\
intelligent_query_router.py \# 查询分析与策略路由\
generation_integration.py \# 证据上下文生成与答案生成\
source_evidence.py \# 引用证据组织\
snippet_service.py \# 原文片段回查\
ui_presenters.py \# 前端展示辅助\
scripts/\
import_fengshen_to_neo4j.py \# 图谱导入入口\
run_e2e_queries.py \# 端到端问答检查脚本\
ui/\
styles.py \# Streamlit 页面样式\
data/fengshen/extraction_results/ \# LLM 抽取缓存与 merged_kg.json\
report_assets/ \# 报告插图资源

## 5.2 环境配置与运行步骤

### 5.2.1 安装依赖

pip install -r requirements.txt

### 5.2.2 配置环境变量

复制 .env.example 为 .env，填写：

SILICONFLOW_API_KEY=sk-xxx\
NEO4J_URI=bolt://localhost:7687\
NEO4J_USER=neo4j\
NEO4J_PASSWORD=12345678

### 5.2.3 导入知识图谱

测试前 3 回：

python scripts/import_fengshen_to_neo4j.py \--chapter-limit 3

全量导入：

python scripts/import_fengshen_to_neo4j.py

如果已有抽取缓存，只写入 Neo4j：

python scripts/import_fengshen_to_neo4j.py \--skip-extraction

只做抽取，不写数据库：

python scripts/import_fengshen_to_neo4j.py \--extraction-only
\--chapter-limit 3

### 5.2.4 启动 Web 问答系统

streamlit run app.py

浏览器打开 Streamlit 页面后点击"初始化系统"，即可进行问答。

## 5.3 当前实现统计

根据当前 data/fengshen/extraction_results/merged_kg.json
抽取缓存统计，样例抽取覆盖前 20 回，共 57 个文本块，生成 350 个实体、420
条关系。实体分布如下：

  -------------------------------------------------------------------
  实体类型                                                       数量
  --------------------------------- ---------------------------------
  Person                                                          128

  Faction                                                          19

  Location                                                         89

  Artifact                                                         51

  Beast                                                             9

  Formation                                                         0

  Event                                                            49

  DeityPosition                                                     5

  **合计**                                                    **350**
  -------------------------------------------------------------------

关系分布中数量较多的类型包括：

  --------------------------------------------------------------------
  关系类型                                 数量 含义
  ---------------------- ---------------------- ----------------------
  FIGHTS_FOR                                 59 效力阵营

  OPPOSES                                    56 对抗关系

  BELONGS_TO_SECT                            45 教派归属

  PARTICIPATES_IN                            42 参与事件

  OWNS                                       40 拥有法宝/坐骑

  KILLS                                      24 击杀

  FATHER_OF                                  18 父子关系

  MARRIED_TO                                 15 婚配关系

  INITIATES                                  14 发起事件

  CHILD_OF                                   14 子女关系
  --------------------------------------------------------------------

需要说明的是，当前缓存是为了开发和验证系统流程生成的前 20
回样例；代码层面支持对 99 回全文进行抽取与导入。即使只从原文 TextChunk
层面看，99 回全文按章节和段落切分也能满足课程要求的 500
条以上非结构化文本数据。

## 5.4 核心 Prompt 展示

### 5.4.1 知识抽取 Prompt 要点

知识抽取 Prompt 的核心内容如下：

你是一位《封神演义》知识图谱构建专家，负责从原文中抽取实体和关系。\
请从文本片段中识别
Person、Faction、Location、Artifact、Beast、Formation、Event、DeityPosition
等实体类型。\
允许的关系类型包括
MASTER_OF、BELONGS_TO_SECT、FIGHTS_FOR、FATHER_OF、KILLS、OWNS、PARTICIPATES_IN、LISTED_ON
等。\
抽取规则：\
1. 只抽取文本片段中明确出现或可严格从片段推断的事实。\
2. 不要凭常识补充文本中没有出现的关系。\
3. 每条关系必须给出 evidence 原文证据。\
4. 人物别名可以放到 alias 数组中。\
5. 输出必须是严格 JSON，不要输出解释。

### 5.4.2 问答生成 Prompt 要点

问答生成 Prompt 的核心内容如下：

你是一位熟悉《封神演义》的知识图谱问答助手。请只基于给定的检索信息回答问题，不要补充外部知识。\
回答要求：\
1. 优先依据每条资料中的"原文摘录"回答。\
2. "图谱事实"只能辅助组织人物、法宝、事件关系，不能替代原文证据。\
3. 每个关键事实后必须标注对应编号，如 \[1\]、\[2\]。\
4. 信息不足时，明确说"根据当前资料无法完全确定"。\
5. 用自然流畅的中文回答，不要输出内部关系代码、chunk_id
或数据库字段名。\
6.
结构化呈现：先直接回答问题，再用"原文依据"列出关键引用编号和对应原文要点。

## 5.5 效果展示与测试案例

![图7
答辩式系统架构与亮点图](media/rId75.png){width="4.899305555555555in"
height="5.470890201224847in"}

图7 答辩式系统架构与亮点图

图7汇总展示了报告中使用的多张系统图，覆盖总体架构、构建流程、问答时序、检索融合、Schema
和简洁流程。实际系统运行时，Streamlit
页面能够展示答案、引用来源和局部知识子图。

### 5.5.1 测试案例一：简单事实查询

  ----------------------------------------------------------------------------------------
  项目                              内容
  --------------------------------- ------------------------------------------------------
  问题                              纣王是谁？

  查询特点                          单实体事实介绍

  纯向量 RAG 预期                   能召回包含纣王身世和朝歌背景的原文片段，回答基本身份

  GraphRAG 预期                     除原文片段外，还能结合 Person
                                    节点、商阵营、相关人物和事件关系，给出结构化回答

  验证重点                          人物简介、原文引用、是否避免输出数据库字段
  ----------------------------------------------------------------------------------------

预期回答要点：纣王是商朝末代君主，又名寿王/殷受/帝辛；都朝歌；与商、帝乙、闻仲、黄飞虎、女娲宫进香等节点存在关系。回答需引用原文中"纣王乃帝乙之三子""随立寿王为天子，名曰纣王，都朝歌"等证据。

### 5.5.2 测试案例二：多跳关系推理

  ----------------------------------------------------------------------------------------------
  项目                              内容
  --------------------------------- ------------------------------------------------------------
  问题                              哪吒的师父是谁？他属于哪个教派？有哪些法宝？

  查询特点                          人物 → 师父 → 教派 → 法宝的多跳查询

  纯向量 RAG 预期                   可能召回多个分散片段，需要模型自行拼接，容易遗漏法宝或教派

  GraphRAG 预期                     沿图谱中的 MASTER_OF、BELONGS_TO_SECT、OWNS 等关系组织答案

  验证重点                          多跳关系链是否完整，是否能展示相关子图
  ----------------------------------------------------------------------------------------------

GraphRAG 的优势在该问题中最明显。系统可以将"哪吒"链接到 Person
节点，检索其师承关系、教派归属和法宝拥有关系，再用原文摘录核验证据。前端子图可展示哪吒、太乙真人、阐教、乾坤圈、混天绫、风火轮等节点及其关系。

### 5.5.3 测试案例三：图谱关系查询

  ----------------------------------------------------------------------------------------------------
  项目                              内容
  --------------------------------- ------------------------------------------------------------------
  问题                              姜子牙和元始天尊是什么关系？

  查询特点                          两个实体之间的关系查询

  纯向量 RAG 预期                   如果召回到对应片段，可以回答师徒关系；如果片段不完整，答案不稳定

  GraphRAG 预期                     直接通过人物节点之间的师承边回答，并补充原文证据

  验证重点                          实体链接、关系边、引用证据和子图展示
  ----------------------------------------------------------------------------------------------------

该问题用于验证图谱实体直连关系。系统应从图谱中提取"元始天尊 ---
MASTER_OF →
姜子牙"或对应反向关系，并在答案中用自然语言说明二者为师徒关系。

### 5.5.4 测试案例四：边界拒答

  ----------------------------------------------------------------------------------------------------------
  项目                              内容
  --------------------------------- ------------------------------------------------------------------------
  问题                              孙悟空在《封神演义》中有什么法宝？

  查询特点                          领域外或证据不足问题

  纯向量 RAG 预期                   可能因神魔题材相似而生成与《西游记》相关的错误答案

  GraphRAG 预期                     如果图谱中没有孙悟空实体或无相关证据，应回答"根据当前资料无法完全确定"

  验证重点                          防幻觉、证据不足时拒答
  ----------------------------------------------------------------------------------------------------------

该问题验证系统的保守回答能力。由于 Prompt
明确要求"只基于检索信息回答"，并且图谱检索能发现实体缺失或关系缺失，GraphRAG
比纯向量 RAG 更容易避免跨作品知识混入。

## 5.6 纯向量 RAG 与 GraphRAG 对比

  ----------------------------------------------------------------------------------------
  维度                   纯向量 RAG                 GraphRAG
  ---------------------- -------------------------- --------------------------------------
  检索对象               文本块                     文本块 + 实体 + 关系 + 路径 + 子图

  适合问题               单段文本能回答的事实问题   多实体、多关系、多跳推理问题

  多跳推理               依赖模型从片段中自行推断   通过图结构显式检索关系链

  可解释性               只能展示相关文本片段       可展示原文片段、三元组和知识子图

  边界拒答               容易受相似文本误导         可通过实体/关系缺失判断证据不足

  系统成本               构建简单，依赖向量索引     需要知识抽取、图数据库和实体对齐

  本项目应用             召回《封神演义》原文片段   组织人物、教派、法宝、事件等关系网络
  ----------------------------------------------------------------------------------------

总体来看，纯向量 RAG 更适合快速构建基础问答系统，而 GraphRAG
更适合本项目这种人物关系复杂、实体类型丰富、需要多跳关系推理和可视化溯源的文学知识问答场景。

# 6. 项目总结和展望

## 6.1 项目总结

本项目完成了一个面向《封神演义》的增强型 GraphRAG
问答系统，主要工作包括：

1.  **完成领域语料处理。** 以《封神演义》99
    回全文为数据源，设计章节解析和文本切块方法，并将原文片段作为
    TextChunk 节点保存，满足 500 条以上非结构化文本数据要求。

2.  **完成大模型知识抽取。** 设计面向《封神演义》的 NER + RE
    Prompt，抽取人物、教派、地点、法宝、坐骑、事件、神位等实体，以及师承、阵营、亲属、对抗、法宝、事件参与、封神结局等关系。

3.  **完成 Neo4j 图谱建模。**
    将实体、关系、章节和原文片段写入图数据库，建立节点索引，并通过 alias
    合并重复实体，实现基础实体对齐。

4.  **完成 FAISS 向量检索。** 将 RAG 文档向量化，保存本地 FAISS
    索引，支持 Top-K 相似文本块检索。

5.  **完成 GraphRAG 检索。**
    实现实体链接、多跳邻居、路径和子图检索，将图谱结构用于关系推理。

6.  **完成智能路由和检索融合。**
    根据问题复杂度和关系密集度自动选择检索策略，融合原文片段与图谱三元组作为上下文。

7.  **完成防幻觉问答生成。** Prompt
    明确要求只基于证据回答、关键事实标注引用编号、证据不足时拒答。

8.  **完成 Web 可视化展示。** Streamlit
    页面展示答案、引用原文和知识子图，提升系统可解释性。

从实现效果看，本系统能够覆盖课程要求的基础层、核心层、应用层和展示层，体现了"知识图谱增强
RAG"的完整技术路线。

## 6.2 存在的问题

当前系统仍存在一些不足：

1.  **知识抽取质量受大模型输出影响。** 大模型可能漏抽、误抽或输出不规范
    JSON；虽然系统提供了重试和 Schema 校验，但仍需要人工抽查和清洗。

2.  **当前缓存样例尚未覆盖全文。** 现有 merged_kg.json 样例覆盖前 20
    回，已经验证系统流程；若用于最终完整展示，应继续抽取 99 回全文。

3.  **实体对齐策略较简单。** 当前主要基于 alias
    合并重复实体，尚未引入向量相似度、规则词典或人工审核机制。

4.  **关系置信度仍较粗。** 当前关系置信度主要由大模型输出 explicit
    等标记，尚未形成可量化评估指标。

5.  **图谱可视化仍可优化。**
    大规模子图展示时可能出现节点过多、布局拥挤的问题，需要进一步设计节点筛选和层级展示策略。

6.  **缺少系统化自动评测集。**
    当前测试案例以典型问题为主，后续可构建标准问答集并量化比较纯向量 RAG
    与 GraphRAG 的准确率、召回率和引用正确率。

## 6.3 后续展望

后续可以从以下方向改进：

1.  **扩展全书知识图谱。** 对 99
    回全文进行完整抽取，进一步丰富人物、法宝、阵法和封神结局关系。

2.  **引入人工校验与主动学习。**
    对低置信度关系进行人工审核，并将审核结果反馈给抽取 Prompt 或规则库。

3.  **增强实体对齐。** 结合 alias、编辑距离、Embedding
    相似度和上下文描述，实现更稳健的同实体合并。

4.  **引入图算法。** 使用
    PageRank、社区发现、路径权重等图算法分析核心人物、阵营结构和事件传播路径。

5.  **建设评测集。**
    构建包含简单事实、多跳关系、路径推理、边界拒答等类型的问题集，系统比较纯向量
    RAG、GraphRAG 和组合策略。

6.  **优化前端体验。**
    增加图谱节点点击查看原文证据、关系过滤、章节筛选和问答历史导出功能。

7.  **拓展到多文本融合。**
    将《封神演义》与其他神魔小说或历史资料对比，构建跨文本文学知识图谱。

综上，本项目不仅完成了课程要求的增强型 RAG
问答系统，也为后续探索"文学文本知识图谱 + 大模型问答 +
图谱可视化溯源"提供了可扩展的工程基础。
