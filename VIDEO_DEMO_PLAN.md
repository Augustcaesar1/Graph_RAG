# 5 分钟演示视频设计流程

视频主题：**基于大模型 + 知识图谱的《封神演义》GraphRAG 问答系统**

建议视频长度：**4 分 50 秒 ~ 5 分 20 秒**。  
核心目标：让老师/评审在 5 分钟内看清楚：项目做了什么、怎么构建图谱、怎么检索推理、怎么展示溯源、相比普通 RAG 有什么优势。

---

## 一、视频总结构

| 时间 | 环节 | 目标 |
|---|---|---|
| 0:00 - 0:25 | 开场介绍 | 说明项目题目、问题背景、系统目标 |
| 0:25 - 1:05 | 数据与知识图谱构建 | 展示原文数据、LLM 抽取、Neo4j 图谱 |
| 1:05 - 1:45 | 系统架构说明 | 展示 GraphRAG 总体架构和模块流程 |
| 1:45 - 2:35 | 简单问答演示 | 展示基础查询、原文引用、答案生成 |
| 2:35 - 3:35 | 多跳推理演示 | 展示 GraphRAG 相比普通 RAG 的核心价值 |
| 3:35 - 4:20 | 边界问题 / 防幻觉演示 | 展示资料不足时拒绝编造 |
| 4:20 - 4:50 | 图谱子图与溯源亮点 | 展示知识图谱可视化和引用来源 |
| 4:50 - 5:00 | 总结收尾 | 总结完成点和项目价值 |

---

## 二、详细分镜脚本

## 0:00 - 0:25 开场介绍

### 画面内容

建议展示：

- PPT 标题页，或 Streamlit 首页截图
- 标题：
  - 《封神演义》GraphRAG 问答系统
  - 基于大模型 + Neo4j 知识图谱 + FAISS 向量检索

### 旁白示例

> 大家好，本项目是一个基于大模型和知识图谱的增强型 RAG 问答系统。  
> 我们选择《封神演义》作为领域语料，通过大模型抽取人物、教派、法宝、阵法、事件等实体和关系，构建 Neo4j 知识图谱。  
> 系统结合 FAISS 向量检索和图谱多跳检索，实现带原文溯源和知识图谱子图展示的问答系统。

### 屏幕操作

不需要操作，展示标题和系统首页即可。

---

## 0:25 - 1:05 数据与知识图谱构建

### 画面内容

依次展示：

1. 项目目录中的 `封神演义.txt`
2. `scripts/import_fengshen_to_neo4j.py`
3. `rag_modules/fengshen_kg_extraction.py` 中的 Prompt 片段
4. Neo4j Browser 中的节点或关系图

### 旁白示例

> 首先是数据处理和知识图谱构建部分。  
> 原始语料是完整的《封神演义》文本，共 99 回，约 60 万字。系统先按照章节和标点进行解析与切块，生成超过 500 个非结构化文本片段。  
> 然后通过大模型 Prompt 进行实体识别和关系抽取，识别人物、教派、地点、法宝、坐骑、阵法、事件和神位等实体类型。  
> 抽取出的关系统一转化为三元组，例如“元始天尊 MASTER_OF 姜子牙”，并写入 Neo4j 图数据库。  
> 同时系统会为节点名称、实体 ID、章节和文本片段建立索引，并基于 alias 做简单实体对齐。

### 屏幕操作建议

可以快速展示以下代码位置，不要停留太久：

- `scripts/import_fengshen_to_neo4j.py`
  - `parse_chapters()`
  - `split_chapter_text()`
  - `_create_indexes()`
  - `_merge_aliased_entities()`
- `rag_modules/fengshen_kg_extraction.py`
  - `NER_RE_SYSTEM_PROMPT`

### 可展示命令

如果环境已经准备好，可以在视频里展示命令，不一定现场执行完整全量导入：

```bash
python scripts/import_fengshen_to_neo4j.py --chapter-limit 3
```

如果不想等待，可以说明：

> 为节省演示时间，这里使用已经抽取好的缓存结果进行图谱导入。

然后展示：

```bash
python scripts/import_fengshen_to_neo4j.py --skip-extraction
```

---

## 1:05 - 1:45 系统架构说明

### 画面内容

展示 `PROJECT_DIAGRAMS.md` 中的系统架构图，或把 Mermaid 图渲染后放入 PPT。

推荐使用：

- “系统总体架构图”
- 或“报告用系统流程简图”

### 旁白示例

> 系统整体分为四个核心部分。  
> 第一部分是离线构建：原文经过章节解析和切块后，一方面进入大模型抽取模块，生成实体关系并写入 Neo4j；另一方面作为 TextChunk 文档用于向量化。  
> 第二部分是向量索引：系统使用 FAISS 存储文本块向量，支持 Top-K 相似文本检索。  
> 第三部分是图谱检索：系统根据问题识别实体，并在 Neo4j 中进行一跳、多跳、路径和子图检索。  
> 第四部分是生成与展示：系统将原文片段和图谱三元组融合为上下文，再由大模型生成带引用编号的答案，最后在 Streamlit 页面展示答案、原文引用和知识图谱子图。

### 屏幕操作

用鼠标依次指向图中的：

- 原文
- LLM 抽取
- Neo4j
- FAISS
- Query Router
- GraphRAG Retrieval
- LLM Answer
- Streamlit UI

---

## 1:45 - 2:35 简单问答演示

### 示例问题

```text
哪吒是谁？
```

### 画面内容

打开 Streamlit 页面：

```bash
streamlit run app.py
```

在页面中输入问题：

```text
哪吒是谁？
```

展示：

- 回答区域
- 引用编号 `[1]`、`[2]`
- 原文片段引用
- 相关图谱节点或三元组

### 旁白示例

> 下面演示一个简单人物查询。  
> 我输入“哪吒是谁？”。系统首先识别问题中的核心实体“哪吒”，然后同时检索相关原文片段和图谱关系。  
> 可以看到，答案不是直接由大模型凭空生成，而是带有引用编号。下面的溯源区域展示了对应的原文片段，说明答案可以回到原始文本进行验证。

### 需要强调

- 这是简单查询
- 重点看“原文依据”
- 展示系统不是无依据生成

---

## 2:35 - 3:35 多跳推理演示

### 示例问题

推荐使用：

```text
哪吒的师父是谁？他属于哪个教派？有哪些法宝？
```

或者：

```text
姜子牙和元始天尊是什么关系？这个关系体现在哪些原文证据中？
```

### 画面内容

在 Streamlit 中输入多跳问题。  
展示：

- 路由策略，如果页面有显示路由决策，打开它
- 答案中的多个事实点
- 图谱三元组
- 子图可视化

### 旁白示例

> 接下来演示 GraphRAG 的核心能力，也就是多跳关系推理。  
> 这个问题不是单纯问一个实体介绍，而是要求系统同时回答师承、教派和法宝多个关系。  
> 普通向量 RAG 可能只能召回相似文本片段，然后依赖大模型自己拼接。GraphRAG 则可以先在图谱中定位“哪吒”节点，再沿着师承、教派归属和法宝拥有关系进行扩展。  
> 因此系统能够把分散在不同文本片段和图谱边中的信息组织起来，形成结构化回答。  
> 这里下方展示的知识图谱子图，也可以看到“哪吒”与师父、教派、法宝之间的关系链路。

### 需要强调

这一段是整个视频重点，建议停留久一点。

强调三点：

1. 问题需要多个关系才能回答
2. 系统使用图谱检索补充普通向量检索
3. 页面展示了三元组或子图，可解释模型依据

---

## 3:35 - 4:20 边界问题 / 防幻觉演示

### 示例问题

```text
孙悟空在封神演义中有什么法宝？
```

### 画面内容

输入边界问题，展示回答。

理想回答应该类似：

> 根据当前资料无法确定 / 当前知识图谱和原文片段中没有检索到孙悟空在《封神演义》中的相关法宝信息。

### 旁白示例

> 第三个演示是边界问题，用来验证系统的防幻觉能力。  
> 这里我问“孙悟空在封神演义中有什么法宝？”。孙悟空并不是《封神演义》的核心人物，如果系统只依赖大模型常识，可能会把《西游记》中的金箍棒等内容混入回答。  
> 本系统的 Prompt 要求模型只能基于检索到的原文和图谱事实回答。  
> 因此当没有足够证据时，系统会明确说明当前资料无法确定，而不是编造答案。

### 需要强调

- 防幻觉是项目要求之一
- 回答必须基于提供的上下文
- 不相关问题要能拒答或说明证据不足

---

## 4:20 - 4:50 图谱子图与溯源亮点展示

### 画面内容

回到刚才的多跳问题结果页面，重点展示：

- 原文引用卡片
- 三元组列表
- 知识图谱子图
- 如果有节点/边交互，可拖动或缩放一下

### 旁白示例

> 最后展示系统的可视化溯源功能。  
> 在每个回答下方，系统会展示对应的原文片段，帮助用户验证答案来源。  
> 同时，系统还会将本次检索到的实体和关系以知识图谱子图形式展示出来，例如人物、教派、法宝、事件之间的连接关系。  
> 这样用户不仅能看到答案，还能看到答案背后的证据和推理路径。

### 画面操作

- 鼠标指向引用片段
- 鼠标指向图谱节点和连线
- 如果图谱可交互，轻微拖动节点

---

## 4:50 - 5:00 总结收尾

### 画面内容

展示总结页，或者停留在系统 UI 页面。

总结页可以写：

```text
项目完成点：
1. 500+ 原文片段处理
2. LLM 实体关系抽取
3. Neo4j 知识图谱构建
4. FAISS + GraphRAG 混合检索
5. 多跳推理问答
6. 原文引用与知识子图溯源
```

### 旁白示例

> 总结来说，本项目完成了从非结构化文本到知识图谱构建，再到向量检索、图谱检索、问答生成和可视化溯源的完整 GraphRAG 流程。  
> 相比普通 RAG，系统在多跳关系查询、结构化推理和答案可解释性方面更有优势。  
> 以上就是本项目的演示，谢谢观看。

---

# 三、推荐录制顺序

建议不要完全现场操作，可以采用“半现场 + 半截图”的方式，保证 5 分钟内流畅完成。

## 推荐顺序

1. 先准备好 Neo4j 数据库和 FAISS 索引
2. 先启动好 Streamlit 页面
3. 打开以下窗口备用：
   - VS Code 项目目录
   - `README.md`
   - `PROJECT_DIAGRAMS.md`
   - Streamlit 页面
   - Neo4j Browser 或 Neo4j Desktop
4. 录制时按以下顺序切换：
   - 标题页
   - 项目目录与数据文件
   - 抽取 Prompt / 导入脚本
   - 架构图
   - Streamlit 简单问答
   - Streamlit 多跳问答
   - Streamlit 边界问题
   - 图谱子图与引用
   - 总结页

---

# 四、录制前检查清单

录制前确认：

- [ ] Neo4j 正在运行
- [ ] `.env` 已配置 API Key 和 Neo4j 连接
- [ ] 已执行过图谱导入脚本
- [ ] Streamlit 可以正常启动
- [ ] 页面点击“初始化系统”后不报错
- [ ] `哪吒是谁？` 能返回答案和引用
- [ ] 多跳问题能返回图谱关系或子图
- [ ] 边界问题不会胡编
- [ ] 浏览器缩放调到 90% 或 100%，文字清晰
- [ ] 录屏分辨率建议 1920×1080
- [ ] 鼠标移动放慢，切换窗口不要太快

---

# 五、推荐演示问题

## 简单查询

```text
哪吒是谁？
```

备选：

```text
姜子牙是谁？
```

## 多跳推理

```text
哪吒的师父是谁？他属于哪个教派？有哪些法宝？
```

备选：

```text
姜子牙和元始天尊是什么关系？这个关系体现在哪些原文证据中？
```

## 图谱关系查询

```text
纣王和妲己之间是什么关系？
```

备选：

```text
闻太师参与了哪些重要事件？
```

## 边界问题

```text
孙悟空在封神演义中有什么法宝？
```

备选：

```text
林黛玉和姜子牙是什么关系？
```

---

# 六、5分钟旁白精简版

如果需要严格控制在 5 分钟，可以直接按下面这版念。

```text
大家好，本项目是一个基于大模型和知识图谱的《封神演义》增强型 RAG 问答系统。系统使用《封神演义》全文作为语料，共 99 回、约 60 万字，并切分为超过 500 个非结构化文本片段。

在图谱构建阶段，系统先按照章节和标点对文本进行解析和切块，然后通过大模型 Prompt 进行实体识别和关系抽取。实体类型包括人物、教派、地点、法宝、坐骑、阵法、事件和神位等；关系包括师承、阵营归属、亲属、对抗、法宝拥有、事件参与和封神结局等。抽取出的知识以三元组形式写入 Neo4j，并建立节点索引，同时基于 alias 做实体对齐。

系统架构上，离线部分负责文本切块、知识抽取、Neo4j 图谱构建和 FAISS 向量索引构建；在线问答部分则由智能路由模块分析用户问题，根据问题复杂度选择向量检索、图谱检索或混合检索。检索结果会被融合成包含原文片段和图谱三元组的上下文，再交给大模型生成答案。

下面演示一个简单查询。我输入“哪吒是谁？”。系统识别出核心实体“哪吒”，检索相关原文片段和图谱关系，并生成带引用编号的回答。可以看到，答案下方展示了对应的原文片段，因此回答可以回到原始文本进行验证。

接下来演示多跳推理。我输入“哪吒的师父是谁？他属于哪个教派？有哪些法宝？”。这个问题需要同时查询师承、教派和法宝关系。普通向量 RAG 主要依赖相似文本召回，而 GraphRAG 可以在 Neo4j 中沿实体关系进行扩展，找到相关节点和多跳关系。因此系统不仅能生成回答，还能展示相关三元组和知识图谱子图。

然后演示防幻觉能力。我输入“孙悟空在封神演义中有什么法宝？”。如果只依赖大模型常识，可能会混入《西游记》的内容。但本系统要求模型只能基于检索到的原文和图谱事实回答，所以在证据不足时会明确说明无法确定，而不是编造答案。

最后看可视化溯源部分。系统在答案下方展示原文引用片段，同时将本次检索到的实体和关系以知识图谱子图形式展示出来。这样用户不仅能看到答案，也能看到答案依据和推理路径。

总结来说，本项目完成了从非结构化文本到知识图谱构建，再到向量检索、图谱检索、问答生成和可视化溯源的完整 GraphRAG 流程。相比普通 RAG，系统在多跳关系查询、结构化推理和答案可解释性方面更有优势。谢谢观看。
```

---

# 七、如果时间不够的压缩方案

如果录制时超过 5 分钟，可以压缩如下：

- 开场压缩到 15 秒
- 数据构建只展示架构图，不打开代码
- 简单查询只展示结果，不逐句解释
- 多跳推理保留完整展示
- 边界问题只展示一句拒答结果
- 总结控制在 10 秒

优先保留：

1. 系统架构
2. 多跳推理
3. 原文引用
4. 图谱子图
5. 防幻觉边界问题
